To Do List
Chairperson
- Click on pending loans must lead to loan approval panel
- Chairperson is a member elevated to Chairperson
- Chairperson must be able to add a member and remove a member


still a white blank page. here are my ChairRoutes.jsx and AppRoutes.jsx files



// src/routes/ChairRoutes.jsx

import { Routes, Route } from "react-router-dom";
import ChairLayout from "../components/layout/ChairLayout";
import Dashboard from "../pages/chairperson/Dashboard";
import LoanApprovalPanel from "../pages/chairperson/LoanApprovalPanel";
import RepaymentOversightPanel from "../pages/chairperson/RepaymentOversightPanel";

const ChairRoutes = () => {
  return (
    <Routes>
      <Route element={<ChairLayout />}>
        <Route index element={<Dashboard />} />
        <Route path="loan-approvals" element={<LoanApprovalPanel />} />
        <Route path="repayments" element={<RepaymentOversightPanel />} />
      </Route>
    </Routes>
  );
};

export default ChairRoutes;

/*import { Routes, Route } from "react-router-dom";
import Dashboard from "../pages/chairperson/Dashboard";
import MemberProfile from "../pages/chairperson/MemberProfile";
import LoanApprovalPanel from "../pages/chairperson/LoanApprovalPanel";
import RepaymentOversightPanel from "../pages/chairperson/RepaymentOversightPanel";

const ChairRoutes = () => {
  return (
    <Routes>
      <Route path="dashboard" element={<Dashboard />} />
      <Route path="members/:memberId" element={<MemberProfile />} />
      <Route path="*" element={<Dashboard />} />
      <Route path="loan-approvals" element={<LoanApprovalPanel />} />
      <Route path="repayments" element={<RepaymentOversightPanel />} />
    </Routes>
  );
};

export default ChairRoutes;

*/

/////////// AppRoutes.jsx ///////////////////
// src/routes/AppRoutes.jsx


import { BrowserRouter, Routes, Route, Navigate } from "react-router-dom";
import { useAuth } from "../context/AuthContext";
import Login from "../pages/auth/Login";
import ChairRoutes from "./ChairRoutes";
import TreasurerRoutes from "./TreasurerRoutes";
import { ROLES } from "../utils/roles";

export default function AppRoutes() {
  const { user } = useAuth();

  if (!user) {
    return (
        <Routes>
          <Route path="*" element={<Login />} />
        </Routes>
    );
  }

  return (
      <Routes>

        {/* Root redirect */}
        <Route
          path="/"
          element={
            user.role === ROLES.CHAIRPERSON
              ? <Navigate to="/chair/dashboard" replace />
              : <Navigate to="/treasurer/dashboard" replace />
          }
        />

        {/* Chair routes */}
        {user.role === ROLES.CHAIRPERSON && (
          <Route path="/chair/*" element={<ChairRoutes />} />
        )}

        {/* Treasurer routes */}
        {user.role === ROLES.TREASURER && (
          <Route path="/treasurer/*" element={<TreasurerRoutes />} />
        )}

        {/* Fallback */}
        <Route path="*" element={<Navigate to="/" replace />} />

</Routes>

  );
}
